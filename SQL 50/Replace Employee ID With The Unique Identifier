SELECT emp.name,
       CASE 
           WHEN uni.unique_id IS NOT NULL 
           THEN uni.unique_id
           ELSE NULL
       END AS unique_id
FROM Employees emp
LEFT JOIN EmployeeUNI uni
ON emp.id = uni.id;
